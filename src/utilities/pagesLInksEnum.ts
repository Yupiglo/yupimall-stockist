export enum LinksEnum {
  home = "/",
  login = "/login",
  logout = "/logout",
  recover = "/recover",

  dashboard = "/dashboard",
  products = "/products",
  orders = "/orders",
  entries = "/entries",
  exits = "/exits",
  deliveries = "/deliveries",
  couriers = "/couriers",
  distributors = "/distributors",
  notifications = "/notifications",
  profile = "/profile",
  scanner = "/scanner",
}
